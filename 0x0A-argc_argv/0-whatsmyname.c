#include <stdio.h>

/**
 * main-print the program name
 * @argv : number of argument
 * @argc : array of argument
 * Return: 0 (success)
*/

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
